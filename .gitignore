# Rust build artifacts
/target

# macOS
.DS_Store

# Editor
.vscode/

# Python (later phases' scripts)
__pycache__/
*.pyc

# Local AI working notes (mirrors the APOST3D convention — delete either
# line if you'd rather track these in the repo, nothing in them is sensitive)
CLAUDE.md
PROJECT-ROADMAP.md

# APOST-3D test data (.fchk/.inp) used for local testing — not committed,
# these are your own calculation files, not project source
*.fchk
*.inp
